# MCP Server Requirements
# Python 3.10+ required

# Core MCP SDK
mcp>=1.0.0

# Async support (usually included with Python 3.10+, but explicitly listed)
# asyncio is built-in, no need to install

# Optional but recommended for development
pytest>=7.4.0
pytest-asyncio>=0.21.0

# For better error handling and logging (optional)
# rich>=13.0.0  # Uncomment for prettier console output